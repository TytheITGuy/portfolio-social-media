# Social Media API (Django + DRF)

A backend-focused social media application built with Django and Django REST Framework.  
This project demonstrates real-world backend concepts including authentication, permissions, relational data modeling, and API design.

---

## 🚀 Features

- User authentication using JWT (SimpleJWT)
- Create, edit, and delete posts (owner-only permissions)
- Like / unlike posts (one like per user per post)
- Public feed with pagination
- User profiles with post and like counts
- `/api/me/` endpoint for authenticated user info
- Clean separation of concerns (models, serializers, views, permissions)

---

## 🧱 Tech Stack

- **Python 3**
- **Django**
- **Django REST Framework**
- **JWT Authentication (SimpleJWT)**
- **SQLite** (development)
- **Git / GitHub**

---

## 🔐 Authentication

This API uses **JWT authentication**.

